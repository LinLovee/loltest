# 🔧 ПОШАГОВАЯ ИНСТРУКЦИЯ ПО УСТАНОВКЕ И ИСПРАВЛЕНИЮ

## ⚠️ ВАЖНО! ОБЯЗАТЕЛЬНО ВЫПОЛНИТЕ ВСЕ ШАГИ!

### Шаг 1: Остановите текущие процессы

```bash
# Остановите все запущенные процессы Node.js
# В терминале где запущен backend нажмите Ctrl+C
# В терминале где запущен frontend нажмите Ctrl+C
```

### Шаг 2: Распакуйте новый архив

```bash
# Удалите старую версию
rm -rf telegram-clone-updated

# Распакуйте новый архив
unzip telegram-clone-final.zip
cd telegram-clone-final
```

### Шаг 3: Установка Backend

```bash
cd backend

# Удалите старые зависимости
rm -rf node_modules package-lock.json

# Установите зависимости заново
npm install

# Проверьте, что папка uploads создана
mkdir -p uploads

# Проверьте что файл .env существует
ls -la .env

# Если .env НЕ существует, создайте его:
cat > .env << 'EOF'
PORT=3000
DATABASE_URL=postgresql://username:password@localhost:5432/telegram_clone
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
CLIENT_URL=http://localhost:5173
EOF

# ⚠️ ВАЖНО: Замените username, password и название БД на ваши реальные данные!
```

### Шаг 4: Установка Frontend

```bash
cd ../frontend

# Удалите старые зависимости
rm -rf node_modules package-lock.json

# Установите зависимости заново
npm install

# Проверьте что файл .env существует
ls -la .env

# Если .env НЕ существует, создайте его:
cat > .env << 'EOF'
VITE_API_URL=http://localhost:3000
VITE_SOCKET_URL=http://localhost:3000
EOF
```

### Шаг 5: Запуск проекта

**Терминал 1 - Backend:**
```bash
cd backend
npm run dev
```

Вы должны увидеть:
```
Server running on port 3000
📁 Uploads path: /путь/к/проекту/backend/uploads
Database initialized successfully
```

**Терминал 2 - Frontend:**
```bash
cd frontend
npm run dev
```

Вы должны увидеть:
```
VITE v5.x.x ready in xxx ms
➜  Local:   http://localhost:5173/
```

### Шаг 6: Проверка работы

1. Откройте http://localhost:5173
2. Зарегистрируйте двух пользователей (в разных браузерах/режимах инкогнито)
3. Отправьте сообщение
4. Попробуйте редактировать (нажмите ✏️ на своём сообщении)
5. Попробуйте удалить (нажмите 🗑️ на своём сообщении)
6. Отправьте фото (📎 → 🖼️ Фото)
7. Отправьте голосовое (нажмите 🎤 и говорите, затем "Отправить")
8. Запишите видео-кружок (📎 → ⭕ Видео-кружок)

### Шаг 7: Проверка логов

**В терминале Backend** вы должны видеть логи при каждом действии:

**При загрузке файла:**
```
📤 Upload request: { receiverId: 2, messageType: 'image', file: { ... } }
💾 Saving to DB: { fileUrl: '/uploads/xxxxx.jpg', messageType: 'image' }
✅ File uploaded successfully
```

**При редактировании:**
```
✏️ Edit message request: { messageId: '5', userId: 1, text: 'Новый текст...' }
✅ Message edited successfully
```

**При удалении:**
```
🗑️ Delete message request: { messageId: '5', userId: 1 }
✅ Message deleted successfully
```

### ❌ Если что-то не работает:

#### Проблема: Файлы не загружаются (пустые)

**Решение:**
```bash
# Проверьте права доступа к папке uploads
cd backend
ls -la uploads/

# Если папки нет, создайте:
mkdir -p uploads
chmod 755 uploads

# Проверьте что в .env правильный CLIENT_URL
cat .env | grep CLIENT_URL
# Должно быть: CLIENT_URL=http://localhost:5173
```

#### Проблема: Не редактируются/удаляются сообщения

**Проверьте:**
1. Открыты ли консоли браузера (F12)
2. Есть ли ошибки в консоли?
3. В Network tab видны ли запросы PUT/DELETE к API?
4. Проверьте что вы редактируете/удаляете СВОИ сообщения (только отправитель может редактировать/удалять)

**В консоли браузера выполните:**
```javascript
localStorage.getItem('token')
// Должен вернуть JWT токен
```

#### Проблема: Видео-кружки не работают

**Решение:**
```bash
# Проверьте что браузер поддерживает MediaRecorder
# В консоли браузера:
navigator.mediaDevices.getUserMedia({ video: true, audio: true })
  .then(() => console.log('✅ Camera/Mic supported'))
  .catch(err => console.log('❌ Error:', err))

# Разрешите доступ к камере и микрофону когда браузер спросит!
```

### 📝 Дополнительные проверки

**Проверка что сервер раздаёт файлы:**
```bash
# После загрузки любого файла, скопируйте имя файла из БД
# И проверьте что файл доступен:
curl http://localhost:3000/uploads/FILENAME

# Должен вернуть содержимое файла, а НЕ ошибку 404
```

**Проверка БД:**
```bash
# Подключитесь к PostgreSQL
psql -U username -d telegram_clone

# Проверьте таблицу messages
SELECT id, sender_id, receiver_id, message_type, file_url, deleted, edited 
FROM messages 
ORDER BY id DESC 
LIMIT 10;

# Вы должны видеть:
# - file_url начинается с /uploads/
# - deleted = false для активных сообщений
# - edited = true для отредактированных
```

## 🎯 Что исправлено

1. ✅ **Загрузка файлов** - путь изменён с `/api/messages/files/` на `/uploads/`
2. ✅ **Статические файлы** - перенесены в `server.js` ПЕРЕД маршрутами
3. ✅ **Голосовые сообщения** - добавлена передача `duration`
4. ✅ **Редактирование** - добавлено логирование для отладки
5. ✅ **Удаление** - добавлено логирование для отладки
6. ✅ **Видео-кружки** - полная реализация с интерфейсом записи

## 🔍 Отладка

Если проблемы остались, отправьте мне:

1. Вывод из консоли backend при попытке загрузить файл
2. Вывод из консоли backend при попытке редактировать сообщение
3. Скриншот консоли браузера (F12 → Console)
4. Скриншот Network tab при отправке файла
5. Результат команды: `ls -la backend/uploads/`

## 📞 Частые вопросы

**В: Почему файлы не скачиваются?**  
О: Проверьте что в БД file_url начинается с `/uploads/`, а не с `/api/messages/files/`

**В: Почему не могу редактировать чужие сообщения?**  
О: Это правильно! Редактировать и удалять можно только СВОИ сообщения.

**В: Видео-кружок не круглый в превью**  
О: Это нормально при записи. После отправки он будет круглым у получателя.

**В: Голосовые не воспроизводятся**  
О: Проверьте что браузер поддерживает WebM аудио. В Chrome/Firefox должно работать.
