# Telegram Clone - Real-time мессенджер

Полнофункциональный мессенджер с real-time общением, построенный на React, Node.js, Socket.io и PostgreSQL.

## 🚀 Возможности

- ✅ Регистрация и аутентификация (JWT)
- ✅ Real-time сообщения через WebSocket (Socket.io)
- ✅ Онлайн статусы пользователей
- ✅ Индикатор печати
- ✅ История сообщений
- ✅ Поиск пользователей
- ✅ Список активных чатов
- ✅ Современный UI в стиле Telegram
- ✅ Адаптивный дизайн (mobile-friendly)

## 📋 Требования

- Node.js 18+
- PostgreSQL база данных (рекомендуется [Neon](https://neon.tech))
- npm или yarn

## 🛠 Локальная установка

### 1. Клонируйте репозиторий

```bash
git clone <your-repo>
cd telegram-clone
```

### 2. Настройка Backend

```bash
cd backend
npm install

# Создайте .env файл
cp .env.example .env
```

Отредактируйте `.env` файл:

```env
DATABASE_URL=postgresql://user:password@host/dbname
JWT_SECRET=your-super-secret-jwt-key-change-this
CLIENT_URL=http://localhost:5173
PORT=3000
```

### 3. Настройка Frontend

```bash
cd ../frontend
npm install

# Создайте .env файл
cp .env.example .env
```

Отредактируйте `.env` файл:

```env
VITE_API_URL=http://localhost:3000
```

### 4. Запуск приложения

В одном терминале (Backend):
```bash
cd backend
npm start
```

В другом терминале (Frontend):
```bash
cd frontend
npm run dev
```

Откройте http://localhost:5173

## 🌐 Деплой на Render.com + Neon.tech

### Шаг 1: Создайте базу данных на Neon.tech

1. Зарегистрируйтесь на [neon.tech](https://neon.tech)
2. Создайте новый проект
3. Скопируйте Connection String (начинается с `postgresql://`)

### Шаг 2: Деплой Backend на Render

1. Зарегистрируйтесь на [render.com](https://render.com)
2. Создайте новый **Web Service**
3. Подключите ваш GitHub репозиторий
4. Настройки:
   - **Name**: telegram-clone-backend
   - **Root Directory**: `backend`
   - **Build Command**: `npm install`
   - **Start Command**: `npm start`

5. Добавьте Environment Variables:
   ```
   DATABASE_URL=postgresql://user:password@ep-xxx.region.aws.neon.tech/dbname
   JWT_SECRET=your-random-secret-key-here
   CLIENT_URL=https://your-frontend-app.onrender.com
   PORT=3000
   ```

6. Нажмите "Create Web Service"

### Шаг 3: Деплой Frontend на Render

1. Создайте новый **Static Site**
2. Подключите тот же репозиторий
3. Настройки:
   - **Name**: telegram-clone-frontend
   - **Root Directory**: `frontend`
   - **Build Command**: `npm install && npm run build`
   - **Publish Directory**: `dist`

4. Добавьте Environment Variable:
   ```
   VITE_API_URL=https://your-backend-app.onrender.com
   ```

5. Нажмите "Create Static Site"

### Шаг 4: Обновите Backend

После деплоя frontend, вернитесь в настройки Backend на Render и обновите:

```
CLIENT_URL=https://your-frontend-app.onrender.com
```

Нажмите "Save Changes" - сервис перезапустится автоматически.

## 📝 Структура проекта

```
telegram-clone/
├── backend/
│   ├── routes/
│   │   ├── auth.js          # Регистрация/вход
│   │   ├── messages.js      # API сообщений
│   │   └── users.js         # API пользователей
│   ├── middleware/
│   │   └── auth.js          # JWT аутентификация
│   ├── db.js                # PostgreSQL подключение
│   ├── server.js            # Express + Socket.io сервер
│   └── package.json
│
└── frontend/
    ├── src/
    │   ├── components/
    │   │   ├── Auth.jsx     # Форма входа/регистрации
    │   │   ├── Chat.jsx     # Главный компонент чата
    │   │   ├── Sidebar.jsx  # Боковая панель с чатами
    │   │   └── ChatWindow.jsx # Окно чата с сообщениями
    │   ├── utils/
    │   │   └── api.js       # API запросы
    │   ├── App.jsx
    │   ├── main.jsx
    │   └── index.css
    └── package.json
```

## 🔐 Безопасность

- Пароли хешируются с помощью bcrypt
- JWT токены для аутентификации
- CORS настроен для определенных доменов
- Все API эндпоинты защищены middleware аутентификацией

## 🎨 Технологии

**Frontend:**
- React 18
- Socket.io Client
- Axios
- Vite

**Backend:**
- Node.js + Express
- Socket.io (WebSocket)
- PostgreSQL
- JWT для auth
- bcrypt для паролей

## 📱 Особенности реализации

### Real-time коммуникация
- Socket.io для мгновенной доставки сообщений
- Онлайн статусы обновляются в реальном времени
- Индикатор "печатает..." работает через WebSocket

### База данных
- Таблицы: users, messages
- Индексы для оптимизации запросов
- Автоматическая инициализация схемы при старте

### UI/UX
- Темная тема в стиле Telegram
- Адаптивный дизайн (desktop + mobile)
- Плавная прокрутка к новым сообщениям
- Непрочитанные сообщения подсвечиваются

## 🐛 Известные ограничения

- Бесплатный тариф Render.com может "засыпать" после 15 минут неактивности (первая загрузка займет ~30 секунд)
- Нет загрузки файлов/изображений (можно добавить позже)
- Нет групповых чатов (только 1-на-1)
- Нет уведомлений push

## 📈 Возможные улучшения

- [ ] Загрузка изображений
- [ ] Групповые чаты
- [ ] Голосовые сообщения
- [ ] Видео звонки
- [ ] End-to-end шифрование
- [ ] Push уведомления
- [ ] Редактирование/удаление сообщений
- [ ] Эмодзи реакции
- [ ] Темы оформления
- [ ] Экспорт чатов

## 📄 Лицензия

MIT

## 🤝 Контрибьюция

Pull requests приветствуются! Не стесняйтесь открывать issues для обсуждения новых фич или багов.
